# Docker Environment Configuration for SpeakInsights

# Whisper Model Configuration
WHISPER_MODEL=base

# Application Mode (full, api, frontend, mcp)
SPEAKINSIGHTS_MODE=full

# Database Configuration
POSTGRES_DB=speakinsights
POSTGRES_USER=speakinsights_user
POSTGRES_PASSWORD=speakinsights_password

# Optional: Override default ports if needed
# STREAMLIT_PORT=8501
# FASTAPI_PORT=8000
# EXTERNAL_API_PORT=3000

# Optional: Model cache settings
# TRANSFORMERS_CACHE=/app/models/transformers
# HF_HOME=/app/models/huggingface
# TORCH_HOME=/app/models/torch